# Webhook URL for notifications
# For signal-cli-rest-api: http://signal-cli-rest-api:8080/v2/send
WEBHOOK_URL=https://your-webhook-url-here

# Webhook Mode: default, signal-cli, or custom
# WEBHOOK_MODE=default

# Optional: Enable debug logging (set to 'true' for verbose output)
# DEBUG=true

# Optional: Number of days to look back (default: 7)
# LOOKBACK_DAYS=7

# Plex Configuration (for cover images)
# These are automatically provided by Tautulli when running as a custom script
# PLEX_URL=http://localhost:32400
# PLEX_TOKEN=your-plex-token

# Signal-cli-rest-api Configuration (when WEBHOOK_MODE=signal-cli)
# Your Signal phone number (sender)
# SIGNAL_NUMBER=+1234567890

# Comma-separated list of recipient phone numbers
# SIGNAL_RECIPIENTS=+0987654321,+1122334455

# Text mode: styled, normal, or extended (default: styled)
# Use "styled" for text formatting (*bold*, _italic_, ~strikethrough~)
# SIGNAL_TEXT_MODE=styled

# Include show covers as base64 attachments (default: false)
# SIGNAL_INCLUDE_COVERS=false

# Signal Message Styling:
# When using signal-cli mode with text_mode=styled, the message will be
# automatically formatted with Signal text styling:
#
# Example output:
# 📺 *3 new seasons* completed in the last 7 days!
#
# • *Breaking Bad* - Season 3 (13 episodes)
# • *The Office* - Season 2 (22 episodes)
# • *Stranger Things* - Season 4 (9 episodes)
#
# _2026-01-31 09:15_
#
# Styling used:
#   *bold text*     - Surround with asterisks
#   _italic text_   - Surround with underscores
#   ~strikethrough~ - Surround with tildes

# Custom Webhook Configuration (when WEBHOOK_MODE=custom)

# Optional: Custom message template
# Available variables: {season_count}, {period_days}, {timestamp}, {show_list}
# WEBHOOK_MESSAGE_TEMPLATE=📺 {season_count} new season(s) completed this week!

# Optional: Send webhook even when no seasons found (default: false)
# WEBHOOK_ON_EMPTY=false

# Optional: Custom JSON payload template with variables:
#   {seasons}, {season_count}, {period_days}, {timestamp}, {message}, {show_list}
# WEBHOOK_PAYLOAD_TEMPLATE={"message": {message}, "count": {season_count}}
